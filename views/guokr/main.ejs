<!DOCTYPE html>
<html lang="en">
<head>
    <title><%= title %></title>
    <% include ../template/header.ejs %>
</head>
<body>
<% include ../template/nav.ejs %>
<div class="container-fluid">
    <div class="row">
        <% if(list.length){ %>
        <% list.forEach(function(item){ %>
        <div class="col-md-3">
            <div class="thumbnail">
                <a href="/guokr/<%= item.id %>"><img class="img-responsive" src="<%= item.img %>" alt=""/></a>
                <div class="caption">
                    <a href="/guokr/<%= item.id %>"><%= item.title %></a>
                </div>
            </div>
        </div>
        <% }); %>
        <% } %>
    </div>

</div>
</body>
</html>